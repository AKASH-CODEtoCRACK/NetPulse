 <div className="relative group">
          
          {/* THE LOCKED OVERLAY (The Glass Blur Effect) */}
          <div className="absolute inset-0 z-20 backdrop-blur-sm bg-[#0B1120]/60 flex flex-col items-center justify-center text-center p-6 border border-slate-800 rounded-2xl transition-opacity duration-500 hover:backdrop-blur-none hover:bg-transparent hover:opacity-0 cursor-pointer group-hover:pointer-events-none">
            <div className="bg-slate-900 p-4 rounded-full border border-slate-700 mb-4 shadow-2xl">
              <Lock size={32} className="text-cyan-400" />
            </div>
            <h3 className="text-2xl font-bold text-white mb-2">Live Threat Feed</h3>
            <p className="text-slate-400 mb-6">Login to view real-time attack vectors targeting your infrastructure.</p>
            <Link href="/dashboard" className="bg-cyan-500 text-black font-bold py-2 px-6 rounded-full hover:scale-105 transition-transform">
              Unlock Dashboard
            </Link>
          </div>

          {/* The Content BEHIND the blur (Visible but blurred) */}
          <div className="bg-[#151e32] rounded-2xl border border-slate-800 p-8 shadow-2xl relative z-10 opacity-50 group-hover:opacity-100 transition-opacity">
            <div className="flex items-center justify-between mb-8 border-b border-slate-700 pb-4">
               <div className="flex items-center gap-2 font-mono text-sm text-slate-400">
                 <div className="w-2 h-2 bg-red-500 animate-pulse rounded-full"></div>
                 LIVE FEED
               </div>
               <div className="text-xs text-slate-500">Auto-refreshing...</div>
            </div>

            {/* Animated Cards */}
            <div className="space-y-4">
              {THREATS.map((threat, idx) => (
                 <div key={idx} className={`p-4 rounded-lg border border-slate-700 bg-slate-800/50 flex items-center gap-4 transition-all duration-500 ${idx === activeThreat ? 'scale-105 border-cyan-500/50 shadow-lg bg-slate-800' : 'opacity-60'}`}>
                    <AlertTriangle className={`
                      ${threat.type === 'critical' ? 'text-red-500' : 'text-amber-500'}
                    `} size={20} />
                    <div>
                      <div className="font-bold text-sm text-slate-200">{threat.msg}</div>
                      <div className="text-xs font-mono text-slate-500">SRC: {threat.ip}</div>
                    </div>
                    {idx === activeThreat && (
                      <span className="ml-auto text-[10px] bg-cyan-500/10 text-cyan-400 px-2 py-1 rounded">Just Now</span>
                    )}
                 </div>
              ))}
              
              {/* Static background items to fill space */}
              <div className="p-4 rounded-lg border border-slate-800 bg-slate-900/30 opacity-40">
                 <div className="h-4 bg-slate-800 rounded w-3/4 mb-2"></div>
                 <div className="h-3 bg-slate-800 rounded w-1/2"></div>
              </div>
            </div>
          </div>

        </div>
      </div>
    </section>
  );
}